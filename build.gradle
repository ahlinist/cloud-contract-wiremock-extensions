task buildExtension(type:Exec) {
	workingDir 'extension'
	commandLine './gradlew', 'clean', 'build', 'install'
}

task buildProject(type:Exec) {
	dependsOn("buildExtension")
	workingDir 'project'
	commandLine './gradlew', 'clean', 'build'
}

task build {
	dependsOn("buildProject")
}